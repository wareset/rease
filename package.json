{
  "name": "rease-monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "description": "This is the monorepo for rease",
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wareset/rease.git"
  },
  "scripts": {
    "build": "lerna run build",
    "prepare": "lerna run build"
  },
  "devDependencies": {
    "lerna": "6.6.2"
  }
}
